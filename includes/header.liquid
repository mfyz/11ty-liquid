<h1>
	Site Title
</h1>

<ul>
	<li>
		<a href="{{ '/' | url }}">
			Home
		</a>
	</li>
	{% for page in collections.pages %}
		<li>
			<a href="{{ page.url | url }}">
				{{ page.data.title }}
			</a>
		</li>
	{% endfor %}
</ul>